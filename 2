#начало программы
#иницализируем массив
#перебираем левые границы
#перебираем элементы от текущей левой границы
#если текущей элемент меньше минимума меняем значения минимума на значение элемента
#меняем минимальный элемент и элемент стоящий на текущей левой границы 
#конец программы

import random
n = 10
l = 0
r = 100
a = [random.randint(l, r) for i in range(n)]
for i in range(n):
    mn = [-1, -1]
    for j in range(i, n):
        if (mn[0] == -1 or mn[0] > a[j]):
            mn = [a[j], j]
    a[i], a[mn[1]] = a[mn[1]], a[i]
print(*a)
